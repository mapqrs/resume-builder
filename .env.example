# No API key required. Tailoring uses your existing Claude Code login via
# `claude -p` (subprocess), or copy-paste mode for full manual control.
#
# This file is kept for forward compatibility — leave it empty unless you
# explicitly want to set ANTHROPIC_API_KEY for the `claude` CLI fallback.
